# FLT-23 E2E Demo Verification

## Programmatic Results (2026-04-25)

All backend API checks passed automatically:

| # | Check | Result |
|---|-------|--------|
| 1 | `GET /health` returns `{"status":"ok"}` | PASS |
| 2 | `GET /current-route` returns 3 couriers, `recalc_duration_ms=0` | PASS |
| 3 | `POST /simulate-anomaly` returns `affected_couriers=3, status="processing"` | PASS |
| 4 | Routes updated post-anomaly (`recalc_duration_ms > 0`) | PASS |
| 5 | All reroutes complete < 2000ms (actual: ~1001ms each) | PASS |
| 6 | WebSocket broadcasts 3 `route_updated` messages with `sequence_id` | PASS |
| 7 | Anomaly flow completes normally when `LINEAR_API_KEY` is empty | PASS |

## Manual Browser Checklist

Run these checks with both servers running (`uvicorn` + `npm run dev`):

### Startup
- [ ] Backend log shows: `OR-Tools warm-up complete`
- [ ] Frontend loads at `http://localhost:3000` without errors
- [ ] Map renders 3 colored route polylines centered on Jakarta

### Live Connection
- [ ] Header shows green pulsing dot + "Live" when backend is up
- [ ] Header shows red dot when backend is stopped

### Simulate Anomaly
- [ ] Click "Simulate: Jalan Sudirman Banjir"
- [ ] Button shows spinner while in-flight
- [ ] Route lines on map update within 2 seconds
- [ ] Audit trail panel shows new entry with `recalc_duration_ms` value

### Linear Integration (requires `LINEAR_API_KEY` in `backend/.env`)
- [ ] URGENT ticket `[ANOMALY] road_closure — sudirman` appears in Linear within 5 seconds
- [ ] Ticket description includes anomaly_id and "Auto-generated by Fleetmind"

### Reconnect & Resilience
- [ ] Refresh browser tab → green dot returns, map shows current routes (not blank)
- [ ] Set `LINEAR_API_KEY=bad_key` in `backend/.env`, restart backend, click Simulate → demo completes normally, no UI error shown
- [ ] Browser DevTools console shows no unhandled errors during full flow

## How to Run

```bash
# Terminal 1 — backend
cd backend && python -m uvicorn backend.main:app --port 8000 --reload

# Terminal 2 — frontend
cd frontend && npm run dev

# Open: http://localhost:3000
```
